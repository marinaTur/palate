# Copy this file to .env and fill in values as needed.
# Never commit .env — it's gitignored.

# Backend endpoint for AI calls (services/ai.js). Required — there is no
# fallback path since Netlify hosting was decommissioned (2026-08-23).
# VITE_API_ENDPOINT=https://functions.yandexcloud.net/d4e5fp0uea63ifeihltc

# Note: the Yandex Metrica counter ID is NOT an env var — it's hardcoded
# directly in index.html's standalone snippet and in useYmPageview.js
# (kept in sync manually between the two). Not a secret — already visible
# in the page's own source.
