# Copy this file to .env and fill in values as needed.
# Never commit .env — it's gitignored.

# Backend endpoint for AI calls (services/ai.js).
# Leave unset for Netlify deploys / local dev — falls back to /api/ask-sommelier.
# Set for Yandex Object Storage deploys, which have no /api/* redirect layer:
# VITE_API_ENDPOINT=https://functions.yandexcloud.net/d4e5fp0uea63ifeihltc
